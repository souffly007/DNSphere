<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <LinearLayout
        android:id="@+id/containerTextEditor"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="12dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/rules_text_hint"
            android:textAppearance="?attr/textAppearanceCaption"
            android:paddingBottom="8dp" />

        <EditText
            android:id="@+id/editRules"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="top|start"
            android:fontFamily="monospace"
            android:textSize="13sp"
            android:inputType="textMultiLine"
            android:hint="@string/rules_text_placeholder"
            android:background="@drawable/bg_code_editor"
            android:padding="8dp"
            android:scrollbars="vertical" />

        <TextView
            android:id="@+id/tvTextStats"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="8dp"
            android:paddingBottom="4dp"
            android:textAppearance="?attr/textAppearanceCaption" />

        <Button
            android:id="@+id/btnApplyText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/rules_apply" />

    </LinearLayout>

    <LinearLayout
        android:id="@+id/containerList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:visibility="gone">

        <TextView
            android:id="@+id/tvListStats"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="12dp"
            android:textAppearance="?attr/textAppearanceCaption" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerRules"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipToPadding="false"
            android:paddingBottom="72dp" />

        <Button
            android:id="@+id/btnAddRule"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="12dp"
            android:text="@string/rules_add_rule" />

    </LinearLayout>

</LinearLayout>